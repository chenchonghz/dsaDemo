<?xml version="1.0" encoding="utf-8"?>
<shape xmlns:android="http://schemas.android.com/apk/res/android" >

    <!-- 颜色  里面-->
    <solid android:color="@color/green_button_selected" />
    <!-- 边框 -->
    <stroke
        android:width="@dimen/green_button_stroke"
        android:color="@color/green_button_selected" />
    <!-- 边距 -->
    <padding
        android:left="@dimen/blue_button_padding"
        android:right="@dimen/blue_button_padding" 
        android:top="@dimen/blue_button_padding"
        android:bottom="@dimen/blue_button_padding"
        />
    <!-- 圆角 -->
    <corners android:radius="@dimen/green_button_radius" />

</shape>